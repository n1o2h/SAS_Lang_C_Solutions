#include <stdio.h> // inclusion  de la bibiotheque contenet foncton printf c'est une directive

int main(void)// point d'entre du program
{
//ce bloc de code se sont  les instructions a executer dans ce program
    printf("Parkinson's Law:\nWork expands so as to ");// \n caractere special il y a aussi \t \\ \"" \' \? \0 \b \a \f \r \v
    printf("fill the time\n");
    printf("available for its completion.\n");
    return 0;
}
/*
l'execution : 
    Parkinson's Law:
    Work expands so as to fill the time
    available for its completion.

*/